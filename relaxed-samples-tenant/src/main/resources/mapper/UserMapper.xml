<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.relaxed.samples.tenant.mapper.UserMapper">


    <select id="myCount" resultType="java.lang.Integer">
        select count(1) from t_user
    </select>

    <select id="listUser" resultType="com.relaxed.samples.tenant.model.User">
        select u.id,u.tenant_id, u.username from t_user u
    </select>
<!--    <select id="listUserAddress" resultType="com.relaxed.samples.tenant.model.User">-->
<!--        select a.name as addr_name, u.id, u.name-->
<!--        from user_addr a-->
<!--                 left join user u on u.id=a.user_id-->
<!--    </select>-->


</mapper>